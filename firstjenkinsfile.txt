pipeline {
    agent any

    tools {
        maven 'apache-maven'  // name provided in "Global Tool Configuration"
    }

    environment {
        IMAGE_NAME = 'yourdockerhubusername/cicddemo'  // Change this
        IMAGE_TAG = 'latest'
    }

    stages {
        stage('Git Clone') {
            steps {
                git branch: 'main',
                credentialsId: 'git_access_token',
                url: 'https://github.com/ferosemfa/cicddemo.git'
            }
        }

        stage('Build with Maven') {
            steps {
                sh 'mvn clean package -DskipTests'  // Builds JAR
            }
        }

	/*
        stage('Build Docker Image') {
            steps {
                script {
                    sh "docker build -t $IMAGE_NAME:$IMAGE_TAG ."
                }
            }
        }
		
        stage('Push Docker Image') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'docker_hub_cred', usernameVariable: 'DOCKER_USER', passwordVariable: 'DOCKER_PASS')]) {
                    script {
                        sh "echo $DOCKER_PASS | docker login -u $DOCKER_USER --password-stdin"
                        sh "docker push $IMAGE_NAME:$IMAGE_TAG"
                    }
                }
            }
        }*/
    }
}
